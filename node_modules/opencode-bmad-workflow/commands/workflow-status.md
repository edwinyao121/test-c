---
description: Show project status — all epics and stories from sprint-status.yaml
---

Call `workflow_status` with no arguments.

Display the result to the user, then suggest next steps:
- Create a new epic → `/workflow-epic`
- Create a story for an existing epic → `/workflow-story`
- Plan a sprint from backlog stories → `/workflow-sprint`
